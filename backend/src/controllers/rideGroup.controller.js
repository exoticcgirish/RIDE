const {
  getAvailableGroups,
  getGroupForRider,
  acceptGroup,
  getAcceptedGroupsForDriver,
  verifyRideOtp,
  completeRide,
} = require("../services/rideGroup.service.js");

/*
|--------------------------------------------------------------------------
| GET AVAILABLE RIDE GROUPS
|--------------------------------------------------------------------------
*/

exports.getAvailable = async (req, res) => {
  try {
    if (!req.user || !req.user.id) {
      return res.status(401).json({
        success: false,
        message: "Authentication required.",
      });
    }

    const groups = await getAvailableGroups(req.user.id);

    const safeGroups = Array.isArray(groups) ? groups : [];

    return res.status(200).json({
      success: true,
      count: safeGroups.length,
      data: safeGroups,
    });
  } catch (error) {
    console.error("Get available groups error:", error);

    return res.status(500).json({
      success: false,
      message:
        error.message ||
        "Failed to get available ride groups.",
    });
  }
};

/*
|--------------------------------------------------------------------------
| GET RIDER'S CURRENT GROUP
|--------------------------------------------------------------------------
*/

exports.getMine = async (req, res) => {
  try {
    if (!req.user || !req.user.id) {
      return res.status(401).json({
        success: false,
        message: "Authentication required.",
      });
    }

    const group = await getGroupForRider(req.user.id);

    return res.status(200).json({
      success: true,
      data: group || null,
    });
  } catch (error) {
    console.error("Get rider group error:", error);

    return res.status(500).json({
      success: false,
      message:
        error.message ||
        "Failed to get rider group.",
    });
  }
};

/*
|--------------------------------------------------------------------------
| ACCEPT RIDE GROUP
|--------------------------------------------------------------------------
|
| ready
|   ↓
| accepted
|
| OTP is generated by the service here.
|
|--------------------------------------------------------------------------
*/

exports.accept = async (req, res) => {
  try {
    if (!req.user || !req.user.id) {
      return res.status(401).json({
        success: false,
        message: "Authentication required.",
      });
    }

    const { groupId } = req.params;

    if (!groupId) {
      return res.status(400).json({
        success: false,
        message: "Group ID is required.",
      });
    }

    const group = await acceptGroup(
      groupId,
      req.user.id,
    );

    return res.status(200).json({
      success: true,
      message: "Ride group accepted successfully.",
      data: group,
    });
  } catch (error) {
    console.error("Accept group error:", error);

    return res.status(400).json({
      success: false,
      message:
        error.message ||
        "Failed to accept ride group.",
    });
  }
};

/*
|--------------------------------------------------------------------------
| GET ACCEPTED / ACTIVE GROUPS FOR DRIVER
|--------------------------------------------------------------------------
|
| Returns:
|
| accepted
| in_progress
|
|--------------------------------------------------------------------------
*/

exports.getAccepted = async (req, res) => {
  try {
    if (!req.user || !req.user.id) {
      return res.status(401).json({
        success: false,
        message: "Authentication required.",
      });
    }

    const groups =
      await getAcceptedGroupsForDriver(
        req.user.id,
      );

    const safeGroups = Array.isArray(groups)
      ? groups
      : [];

    return res.status(200).json({
      success: true,
      count: safeGroups.length,
      data: safeGroups,
    });
  } catch (error) {
    console.error(
      "Get accepted groups error:",
      error,
    );

    return res.status(500).json({
      success: false,
      message:
        error.message ||
        "Failed to get accepted ride groups.",
    });
  }
};

/*
|--------------------------------------------------------------------------
| VERIFY RIDE OTP
|--------------------------------------------------------------------------
|
| Driver enters OTP provided by rider.
|
| accepted
|    ↓
| verify OTP
|    ↓
| in_progress
|
|--------------------------------------------------------------------------
*/

exports.verifyOtp = async (req, res) => {
  try {
    if (!req.user || !req.user.id) {
      return res.status(401).json({
        success: false,
        message: "Authentication required.",
      });
    }

    const { groupId } = req.params;

    const { otp } = req.body;

    if (!groupId) {
      return res.status(400).json({
        success: false,
        message: "Group ID is required.",
      });
    }

    if (!otp) {
      return res.status(400).json({
        success: false,
        message: "OTP is required.",
      });
    }

    const group = await verifyRideOtp(
      groupId,
      req.user.id,
      otp,
    );

    return res.status(200).json({
      success: true,
      message: "OTP verified. Ride started successfully.",
      data: group,
    });
  } catch (error) {
    console.error("Verify ride OTP error:", error);

    return res.status(400).json({
      success: false,
      message:
        error.message ||
        "Failed to verify ride OTP.",
    });
  }
};

/*
|--------------------------------------------------------------------------
| COMPLETE RIDE
|--------------------------------------------------------------------------
|
| in_progress
|      ↓
| completed
|
|--------------------------------------------------------------------------
*/

exports.complete = async (req, res) => {
  try {
    if (!req.user || !req.user.id) {
      return res.status(401).json({
        success: false,
        message: "Authentication required.",
      });
    }

    const { groupId } = req.params;

    if (!groupId) {
      return res.status(400).json({
        success: false,
        message: "Group ID is required.",
      });
    }

    const group = await completeRide(
      groupId,
      req.user.id,
    );

    return res.status(200).json({
      success: true,
      message: "Ride completed successfully.",
      data: group,
    });
  } catch (error) {
    console.error("Complete ride error:", error);

    return res.status(400).json({
      success: false,
      message:
        error.message ||
        "Failed to complete ride.",
    });
  }
};